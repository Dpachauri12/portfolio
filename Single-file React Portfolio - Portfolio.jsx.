import React from "react";
import { motion } from "framer-motion";
import { Mail, Github, Linkedin } from "lucide-react";

// Single-file, production-ready React portfolio component using TailwindCSS.
// How to use:
// 1. Add TailwindCSS to your project (create-react-app, Vite, Next.js, etc.).
// 2. Install framer-motion and lucide-react if you want the animations and icons:
//    npm install framer-motion lucide-react
// 3. Drop this file in your src/ folder and import it in App.jsx: import Portfolio from './Portfolio';

const projects = [
  {
    title: "Hand Gesture Recognition Game",
    description:
      "A Unity + React front-end that uses a webcam and a CV model to detect hand gestures and control gameplay.",
    tech: ["React", "Unity", "TensorFlow.js"],
    link: "#",
  },
  {
    title: "Placement Prep Dashboard",
    description:
      "A dashboard to track DSA progress, daily timetable, and mock interview scores. Includes analytics and reminders.",
    tech: ["React", "Node.js", "Chart.js"],
    link: "#",
  },
  {
    title: "Computer Vision Intern Portfolio",
    description:
      "Samples of image-processing projects (edge-detection, object-tracking) and internship-ready write-ups.",
    tech: ["Python", "OpenCV", "PyTorch"],
    link: "#",
  },
];

const skills = [
  "JavaScript / TypeScript",
  "React / Next.js",
  "C++ / DSA",
  "Python / OpenCV",
  "HTML & CSS (Tailwind)",
  "Git & GitHub",
];

export default function Portfolio() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-white via-slate-50 to-slate-100 text-slate-900">
      <header className="max-w-5xl mx-auto px-6 py-6 flex items-center justify-between">
        <h1 className="text-xl font-extrabold tracking-tight">Daksh Pachauri</h1>
        <nav className="flex gap-4 items-center">
          <a href="#projects" className="text-sm hover:underline">
            Projects
          </a>
          <a href="#skills" className="text-sm hover:underline">
            Skills
          </a>
          <a href="#contact" className="text-sm hover:underline">
            Contact
          </a>
        </nav>
      </header>

      <main className="max-w-5xl mx-auto px-6 pb-24">
        {/* Hero */}
        <section className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center py-12">
          <motion.div
            initial={{ opacity: 0, y: 8 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.45 }}
          >
            <p className="uppercase text-sm tracking-widest text-slate-500">Hi, I’m</p>
            <h2 className="text-4xl md:text-5xl font-extrabold leading-tight mt-2">
              Daksh Pachauri
            </h2>
            <p className="mt-4 text-slate-700 max-w-xl">
              I build interactive web apps and computer vision projects. I'm a 7th
              semester student preparing for campus placements — focused on DSA,
              system design basics, and real-world CV applications.
            </p>

            <div className="mt-6 flex gap-3">
              <a
                href="#contact"
                className="inline-flex items-center px-4 py-2 rounded-2xl bg-slate-900 text-white font-medium shadow hover:opacity-95"
              >
                Contact Me
              </a>
              <a
                href="#projects"
                className="inline-flex items-center px-4 py-2 rounded-2xl border border-slate-200 bg-white text-slate-900 font-medium shadow-sm"
              >
                See Projects
              </a>
            </div>

            <div className="mt-6 flex items-center gap-3 text-slate-600">
              <a href="#" aria-label="Github" className="hover:text-slate-900">
                <Github size={18} />
              </a>
              <a href="#" aria-label="LinkedIn" className="hover:text-slate-900">
                <Linkedin size={18} />
              </a>
              <a href="#contact" aria-label="Email" className="hover:text-slate-900">
                <Mail size={18} />
              </a>
            </div>
          </motion.div>

          <motion.div
            className="bg-gradient-to-br from-slate-50 to-white rounded-2xl p-6 shadow-lg"
            initial={{ opacity: 0, scale: 0.98 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.5 }}
          >
            <div className="w-full h-56 md:h-64 rounded-xl bg-[url('https://images.unsplash.com/photo-1522199710521-72d69614c702?q=80&w=1080&auto=format&fit=crop&ixlib=rb-4.0.3&s=')] bg-cover bg-center" />
            <div className="mt-4">
              <h3 className="font-semibold">Quick facts</h3>
              <ul className="mt-2 text-sm text-slate-600 space-y-1">
                <li>📍 Based in India</li>
                <li>🎓 7th semester student — preparing for placements</li>
                <li>💼 Looking for internships in computer vision / web</li>
              </ul>
            </div>
          </motion.div>
        </section>

        {/* Projects */}
        <section id="projects" className="pt-8">
          <h3 className="text-2xl font-bold">Selected Projects</h3>
          <p className="mt-2 text-slate-600 max-w-2xl">
            A curated selection of projects demonstrating full-stack web apps,
            computer vision experiments, and placement-prep utilities.
          </p>

          <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-6">
            {projects.map((p, idx) => (
              <motion.article
                key={p.title}
                className="rounded-2xl p-6 bg-white border border-slate-100 shadow-sm"
                initial={{ opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: idx * 0.06 }}
              >
                <h4 className="font-semibold text-lg">{p.title}</h4>
                <p className="mt-2 text-sm text-slate-600">{p.description}</p>
                <div className="mt-4 flex flex-wrap gap-2">
                  {p.tech.map((t) => (
                    <span
                      key={t}
                      className="text-xs px-2 py-1 rounded-full border border-slate-200"
                    >
                      {t}
                    </span>
                  ))}
                </div>
                <div className="mt-4 flex items-center gap-3">
                  <a
                    href={p.link}
                    className="text-sm font-medium text-slate-900 hover:underline"
                  >
                    View
                  </a>
                  <a href="#contact" className="text-sm text-slate-500">
                    Ask about this
                  </a>
                </div>
              </motion.article>
            ))}
          </div>
        </section>

        {/* Skills */}
        <section id="skills" className="mt-12">
          <h3 className="text-2xl font-bold">Skills</h3>
          <p className="mt-2 text-slate-600 max-w-2xl">Technologies and topics I work with frequently.</p>

          <div className="mt-6 flex flex-wrap gap-3">
            {skills.map((s) => (
              <div key={s} className="px-3 py-2 rounded-xl bg-white border border-slate-100 shadow-sm text-sm">
                {s}
              </div>
            ))}
          </div>
        </section>

        {/* Contact */}
        <section id="contact" className="mt-16 rounded-2xl p-6 bg-white border border-slate-100 shadow-md">
          <h3 className="text-2xl font-bold">Get in touch</h3>
          <p className="mt-2 text-slate-600">I’m happy to talk about internships, collaborations, or placement prep.</p>

          <form className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
            <input
              aria-label="Name"
              placeholder="Your name"
              className="col-span-1 md:col-span-1 rounded-xl border border-slate-200 p-3"
            />
            <input
              aria-label="Email"
              placeholder="you@domain.com"
              className="col-span-1 md:col-span-1 rounded-xl border border-slate-200 p-3"
            />
            <textarea
              aria-label="Message"
              placeholder="Tell me what you want to work on"
              className="col-span-1 md:col-span-2 rounded-xl border border-slate-200 p-3 h-28"
            />
            <button type="submit" className="col-span-1 md:col-span-2 inline-flex items-center justify-center px-4 py-2 rounded-2xl bg-slate-900 text-white font-medium">
              Send message
            </button>
          </form>
        </section>

        {/* Footer */}
        <footer className="mt-12 py-8 text-sm text-slate-500 flex items-center justify-between">
          <div>© {new Date().getFullYear()} Daksh Pachauri — Built with React + Tailwind</div>
          <div className="flex items-center gap-4">
            <a href="#">Privacy</a>
            <a href="#">Terms</a>
          </div>
        </footer>
      </main>
    </div>
  );
}
