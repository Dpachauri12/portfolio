import React from "react";
import { motion } from "framer-motion";
import { Mail, Github, Linkedin } from "lucide-react";

// Production-ready single-file React portfolio (TailwindCSS + Framer Motion)
// Install deps: npm install framer-motion lucide-react
// Place this file in src/ and import in App.jsx: import Portfolio from "./Portfolio";

const projects = [
  {
    title: "Predicting Bikeshare Patterns",
    description:
      "Developed a predictive modeling system to analyze and forecast bicycle usage patterns in bike-sharing systems. Implemented CNN-based deep learning models with data preprocessing and feature engineering to improve prediction accuracy at city and station levels.",
    tech: ["Python", "CNNs", "Predictive Modeling", "Data Analysis"],
    link: "https://github.com/Dpachauri12/bikeshare-prediction",
  },
  {
    title: "Hand Gesture Gaming Application",
    description:
      "Developing a real-time gesture-controlled gaming application using computer vision. Enabled gameplay without traditional input devices and optimized browser performance using Unity WebGL with a React-based UI.",
    tech: ["React.js", "Unity WebGL", "Computer Vision"],
    link: "https://github.com/Dpachauri12/hand-gesture-game",
  },
  {
    title: "Personal Portfolio Website",
    description:
      "Designed and deployed a responsive personal portfolio website using React and TailwindCSS to showcase projects, skills, and achievements with a recruiter-friendly UI.",
    tech: ["React.js", "HTML", "CSS", "Tailwind"],
    link: "https://github.com/Dpachauri12/portfolio",
  },
];

const skills = [
  "C++",
  "Java",
  "Python",
  "SQL (MySQL)",
  "React.js",
  "HTML, CSS, JavaScript",
  "Computer Vision",
  "CNNs & Predictive Modeling",
  "Data Structures & OOPs",
  "DBMS, OS, Computer Networks",
  "Git & GitHub",
  "Unity WebGL",
];

export default function Portfolio() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-white via-slate-50 to-slate-100 text-slate-900">
      <header className="max-w-5xl mx-auto px-6 py-6 flex items-center justify-between">
        <h1 className="text-xl font-extrabold tracking-tight">Daksh Pachauri</h1>
        <nav className="flex gap-4 items-center">
          <a href="#projects" className="text-sm hover:underline">
            Projects
          </a>
          <a href="#skills" className="text-sm hover:underline">
            Skills
          </a>
          <a href="#contact" className="text-sm hover:underline">
            Contact
          </a>
        </nav>
      </header>

      <main className="max-w-5xl mx-auto px-6 pb-24">
        {/* Hero */}
        <section className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center py-12">
          <motion.div
            initial={{ opacity: 0, y: 8 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.45 }}
          >
            <p className="uppercase text-sm tracking-widest text-slate-500">Hi, I’m</p>
            <h2 className="text-4xl md:text-5xl font-extrabold leading-tight mt-2">
              Daksh Pachauri
            </h2>
            <p className="mt-4 text-slate-700 max-w-xl">
              Final-year B.Tech CSE (AI & ML) student with strong software engineering fundamentals.
              Experienced in Python, C++, Java, SQL, React, and Computer Vision.
              I build real-world applications and ML models for predictive analytics and interactive systems.
            </p>

            <div className="mt-6 flex gap-3">
              <a
                href="#contact"
                className="inline-flex items-center px-4 py-2 rounded-2xl bg-slate-900 text-white font-medium shadow hover:opacity-95"
              >
                Contact Me
              </a>
              <a
                href="#projects"
                className="inline-flex items-center px-4 py-2 rounded-2xl border border-slate-200 bg-white text-slate-900 font-medium shadow-sm"
              >
                See Projects
              </a>
              <a
                href="/Daksh_Pachauri_Resume.pdf"
                target="_blank"
                className="inline-flex items-center px-4 py-2 rounded-2xl border border-slate-200 bg-white text-slate-900 font-medium shadow-sm"
              >
                Download Resume
              </a>
            </div>

            <div className="mt-6 flex items-center gap-3 text-slate-600">
              <a
                href="https://github.com/Dpachauri12"
                target="_blank"
                rel="noreferrer"
                aria-label="Github"
                className="hover:text-slate-900"
              >
                <Github size={18} />
              </a>
              <a
                href="https://www.linkedin.com/in/daksh-pachauri"
                target="_blank"
                rel="noreferrer"
                aria-label="LinkedIn"
                className="hover:text-slate-900"
              >
                <Linkedin size={18} />
              </a>
              <a
                href="mailto:dakshpachauri@gmail.com"
                aria-label="Email"
                className="hover:text-slate-900"
              >
                <Mail size={18} />
              </a>
            </div>
          </motion.div>

          <motion.div
            className="bg-gradient-to-br from-slate-50 to-white rounded-2xl p-6 shadow-lg"
            initial={{ opacity: 0, scale: 0.98 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.5 }}
          >
            <div className="w-full h-56 md:h-64 rounded-xl bg-[url('https://images.unsplash.com/photo-1522199710521-72d69614c702?q=80&w=1080&auto=format&fit=crop')] bg-cover bg-center" />
            <div className="mt-4">
              <h3 className="font-semibold">Quick facts</h3>
              <ul className="mt-2 text-sm text-slate-600 space-y-1">
                <li>📍 Greater Noida, UP, India</li>
                <li>🎓 Final-year B.Tech CSE (AI & ML), 2026</li>
                <li>💼 Seeking full-time Software Engineer / Data Analyst roles</li>
              </ul>
            </div>
          </motion.div>
        </section>

        {/* Projects */}
        <section id="projects" className="pt-8">
          <h3 className="text-2xl font-bold">Selected Projects</h3>
          <p className="mt-2 text-slate-600 max-w-2xl">
            Real-world machine learning and computer vision projects with production-focused implementation.
          </p>

          <div className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-6">
            {projects.map((p, idx) => (
              <motion.article
                key={p.title}
                className="rounded-2xl p-6 bg-white border border-slate-100 shadow-sm"
                initial={{ opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: idx * 0.06 }}
              >
                <h4 className="font-semibold text-lg">{p.title}</h4>
                <p className="mt-2 text-sm text-slate-600">{p.description}</p>
                <div className="mt-4 flex flex-wrap gap-2">
                  {p.tech.map((t) => (
                    <span
                      key={t}
                      className="text-xs px-2 py-1 rounded-full border border-slate-200"
                    >
                      {t}
                    </span>
                  ))}
                </div>
                <div className="mt-4 flex items-center gap-3">
                  <a
                    href={p.link}
                    target="_blank"
                    rel="noreferrer"
                    className="text-sm font-medium text-slate-900 hover:underline"
                  >
                    View on GitHub
                  </a>
                </div>
              </motion.article>
            ))}
          </div>
        </section>

        {/* Skills */}
        <section id="skills" className="mt-12">
          <h3 className="text-2xl font-bold">Skills</h3>
          <p className="mt-2 text-slate-600 max-w-2xl">
            Core technologies and computer science fundamentals I work with.
          </p>

          <div className="mt-6 flex flex-wrap gap-3">
            {skills.map((s) => (
              <div
                key={s}
                className="px-3 py-2 rounded-xl bg-white border border-slate-100 shadow-sm text-sm"
              >
                {s}
              </div>
            ))}
          </div>
        </section>

        {/* Contact */}
        <section id="contact" className="mt-16 rounded-2xl p-6 bg-white border border-slate-100 shadow-md">
          <h3 className="text-2xl font-bold">Get in touch</h3>
          <p className="mt-2 text-slate-600">
            Open to full-time roles, internships, and project collaborations.
          </p>

          <form
            onSubmit={(e) => {
              e.preventDefault();
              alert("Thanks for reaching out! I’ll get back to you soon.");
            }}
            className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4"
          >
            <input
              aria-label="Name"
              placeholder="Your name"
              className="rounded-xl border border-slate-200 p-3"
            />
            <input
              aria-label="Email"
              placeholder="you@domain.com"
              className="rounded-xl border border-slate-200 p-3"
            />
            <textarea
              aria-label="Message"
              placeholder="Tell me what you want to work on"
              className="col-span-1 md:col-span-2 rounded-xl border border-slate-200 p-3 h-28"
            />
            <button
              type="submit"
              className="col-span-1 md:col-span-2 inline-flex items-center justify-center px-4 py-2 rounded-2xl bg-slate-900 text-white font-medium"
            >
              Send message
            </button>
          </form>
        </section>

        {/* Footer */}
        <footer className="mt-12 py-8 text-sm text-slate-500 flex items-center justify-between">
          <div>© {new Date().getFullYear()} Daksh Pachauri — Built with React + Tailwind</div>
          <div className="flex items-center gap-4">
            <a href="https://www.linkedin.com/in/daksh-pachauri" target="_blank" rel="noreferrer">
              LinkedIn
            </a>
            <a href="https://github.com/Dpachauri12" target="_blank" rel="noreferrer">
              GitHub
            </a>
          </div>
        </footer>
      </main>
    </div>
  );
}
